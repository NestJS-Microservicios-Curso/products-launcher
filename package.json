{
  "scripts": {
    "dev": "docker compose -f docker-compose.yml -f docker-compose.watch.yml up --build --watch nats-server products-ms orders-ms payments-ms auth-ms client-gateway",
    "prod": "docker compose -f docker-compose.yml up --build"
  }
}